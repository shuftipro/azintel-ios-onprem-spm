# azintel-ios-onprem-spm
This repository contains the AZIntel iOS On-Premise SDK, configured for distribution and integration through Swift Package Manager (SPM).  The repository is maintained separately from the standard/public iOS SDK and contains the required configuration, package structure, and implementation for On-Premise deployments.
